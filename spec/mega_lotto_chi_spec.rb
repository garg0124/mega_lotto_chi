# frozen_string_literal: true

RSpec.describe MegaLottoChi do
  it "has a version number" do
    expect(MegaLottoChi::VERSION).not_to be nil
  end

  # it "does something useful" do
  #   expect(false).to eq(true)
  # end
end
